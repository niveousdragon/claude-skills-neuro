# Скиллы для работы с Claude — для нейробиологов

Набор «скиллов» (готовых методичек для Claude Code) и короткий гайд, как с ними
работать в нейронауке: анализ данных, фигуры для статей, литература.

Техническая часть — установка, плагины, команды — вынесена в `CLAUDE.md` и
предназначена для самого Клода. Тебе её читать не нужно: попроси «поставь скиллы
из этого репозитория **по его CLAUDE.md**» — он прочитает инструкцию и сделает всё
сам.

---

## Что такое «скилл» и зачем это нужно

Скилл — это инструкция «как делать X правильно», которую Клод **сам подтягивает**,
когда видит подходящую задачу. В отличие от обычного промпта, его не надо
вставлять заново каждый раз: написал один раз — пользуется вся лаборатория,
результат одинаковый у всех.

Грубо: **промпт — сказать один раз; скилл — научить навсегда.**

---

## Как начать

1. Установи Claude Code (если ещё нет).
2. Дай Клоду одну из формулировок ниже — **обязательно со словами «по CLAUDE.md»**
   (так он точно прочитает инструкцию по установке):

   - Если **открыл эту папку** в Claude Code:
     > **«Поставь скиллы из этого репозитория по его CLAUDE.md (полная установка)».**
   - Если работаешь **в другом проекте** (папка не открыта):
     > **«Склонируй https://github.com/niveousdragon/claude-skills-neuro и выполни
     > полную установку по его CLAUDE.md».**

Клод прочитает инструкции в `CLAUDE.md` и поставит **весь набор сразу** — наши
кастомные скиллы плюс рекомендованные наборы (рабочий процесс, научные статьи,
ресёрч).

### Что нужно от тебя руками

Почти всё Клод делает сам. Единственное, что обычно приходится сделать вручную, —
вставить несколько команд установки плагинов. Тут есть две ловушки, на которых
легко споткнуться:

- **Вводи команды по одной за раз.** Вставил строку → Enter → следующая. Несколько
  команд в одну строку через пробел **не сработают**.
- **Каждая строка должна начинаться с `/`** (символ `/` — самый первый, **без
  пробела перед ним**). Пробел в начале → команда станет обычным текстом и не
  выполнится.

В конце один раз выполни `/reload-plugins`. Если что-то не появилось — перезапусти
Claude Code.

**Проверка:** спроси «Какие скиллы тебе доступны?» — в списке должны быть
`deep-analysis`, `figure-alignment`, `tex-to-docx`, скиллы из `superpowers`,
команды `/ars-*` и `skill-creator`.

---

## Что входит в набор

Установка подключает несколько групп возможностей. Какие именно скиллы внутри —
знать не нужно: просто описывай задачу своими словами, и Клод сам подтянет
подходящий.

- **Анализ данных и фигуры** — наши кастомные скиллы для работы с нейроданными и
  публикационными фигурами.
- **Статья из LaTeX в Word** — отдать текст соавтору или в журнал, ничего не
  потеряв: картинки, формулы, таблицы и список литературы на месте, черновые
  комментарии не видны.
- **Рабочий процесс** — как думать над задачей: разложить замысел до кода,
  дисциплина отладки, планы по шагам, «не объявлять готово без доказательств».
- **Научные статьи** — план статьи по главам, обзор литературы, **проверка цитат**
  на выдуманные ссылки, симуляция рецензии, конвертация LaTeX/DOCX/PDF.
- **Глубокий ресёрч** — основательный обзор темы с проверяемыми источниками.

---

## Рецепты — типовые запросы

Скопируй формулировку, подставь своё.

- **Фигура для статьи:** «Собери публикационную фигуру: N панелей, такая-то
  раскладка. Проверь выравнивание программно, а не на глаз».
- **Странный результат:** «Расследуй, вот что не так. Нужны цифры и графики, код не
  трогай — только предложи изменения».
- **Медленный пайплайн:** «Профилируй вот это, найди узкие места, сравни
  альтернативы».
- **Литература:** «Сделай обзор литературы по теме …», затем «помоги выстроить
  структуру раздела Methods».
- **Проверить цитаты:** «Проверь все ссылки в этом .bib/.tex на выдуманные,
  неверные годы и авторов».
- **Статью в Word:** «Переведи вот эту .tex-статью в Word для соавтора, который не
  работает с LaTeX. Картинки, формулы, таблицы и ссылки должны быть на месте,
  черновых комментариев быть не должно».
- **Чужой код:** «Объясни, что делает этот модуль, нарисуй поток данных, отметь
  подозрительные места. Не меняй код».

---

## Принципы «как клодить»

1. **Заведи `CLAUDE.md` в своём проекте** — память проекта: как запускать тесты,
   соглашения, что нельзя трогать. Клод читает её каждую сессию.
2. **Давай контекст, а не только команду.** «Вот ошибка, вот что я ждал, вот как
   воспроизвести» сильнее, чем «почини это».
3. **Требуй доказательства.** «Готово» без прогона теста ничего не значит — проси
   вывод команды, цифры, до/после.
4. **Не давай выдумывать числа и ссылки.** Любая цифра — из реального прогона;
   любая ссылка — проверена. Нет данных — пусть скажет «не знаю».
5. **Итерируй маленькими шагами** с согласованием плана, а не «сделай всё разом».
6. **Тяжёлые расследования веди в изолированном режиме** — пусть Клод разбирается
   в отдельной папке, не трогая рабочий код, и в конце предложит изменения.
7. **Записывай грабли** в скиллы и `CLAUDE.md`, чтобы лаборатория не наступала на
   них снова.

---

## Хочу свой скилл

Файлы создавать и трогать терминал не нужно — это сделает Клод. Если ты ловишь
себя на том, что объясняешь Клоду одно и то же по многу раз, попроси превратить
это в скилл:

> «Я каждый раз объясняю тебе, как у нас принято <делать то-то>. Сделай из этого
> скилл».

Расскажи своими словами две вещи: **когда** этот приём нужен (по каким словам/
ситуациям его доставать) и **как** делать правильно. Клод напишет `SKILL.md`,
положит в нужное место и проверит, что скилл подхватился.

Чтобы поделиться с лабораторией, добавь:

> «И добавь этот скилл в наш общий репозиторий со скиллами».

Клод сам положит его в `skills/` и закоммитит — остальным он станет доступен после
обновления репозитория. Если хочешь сделать скилл основательно, с проверкой на
устойчивость, попроси Клода «использовать `writing-skills`» (из набора
`superpowers`).

---

## Что в репозитории

```
claude-skills-neuro/
├── README.md                       ← этот файл (для человека)
├── CLAUDE.md                       ← инструкции для Клода (установка, плагины)
├── skills/                         ← кастомные скиллы (по папке на скилл)
├── install.ps1                     ← установка (Windows)
└── install.sh                      ← установка (macOS / Linux)
```
